# Optional default-profile/CI fallback. Prefer `clipit login` for interactive use.
# Never place an enterprise workspace key here; store it in a named CLI profile.
CLIPPER_API_KEY=clipper_YOUR_KEY_HERE
CLIPPER_BASE_URL=https://clipit.dev
